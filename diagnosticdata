def get_service_did_hex(service) -> str:
    """
    Extract DID from a service by finding the DATA-ID param
    in the request definition.
    """
    if not service or not service.request:
        return ""

    for p in service.request.params:
        if p.semantic == "DATA-ID" and p.codedConstValue:
            try:
                return f"0x{int(p.codedConstValue, 16):04X}"
            except ValueError:
                return p.codedConstValue

    return ""

# --- CODED-CONST handling (xsi:type OR child element) ---
xsi_type = get_attr_ci(param_el, 'xsi:type', 'type')

coded_value = ''

# Case 1: xsi:type="CODED-CONST"
if xsi_type.upper() == 'CODED-CONST':
    coded_value = extract_coded_value(param_el)

# Case 2: explicit <CODED-CONST> child
else:
    codedConst = find_child(param_el, 'CODED-CONST')
    if codedConst is not None:
        coded_value = extract_coded_value(codedConst)

